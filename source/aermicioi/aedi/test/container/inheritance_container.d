/**
License:
	Boost Software License - Version 1.0 - August 17th, 2003

	Permission is hereby granted, free of charge, to any person or organization
	obtaining a copy of the software and accompanying documentation covered by
	this license (the "Software") to use, reproduce, display, distribute,
	execute, and transmit the Software, and to prepare derivative works of the
	Software, and to permit third-parties to whom the Software is furnished to
	do so, all subject to the following:
	
	The copyright notices in the Software and this entire statement, including
	the above license grant, this restriction and the following disclaimer,
	must be included in all copies of the Software, in whole or in part, and
	all derivative works of the Software, unless such copies or derivative
	works are solely in the form of machine-executable object code generated by
	a source language processor.
	
	THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
	IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
	FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
	SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
	FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
	ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
	DEALINGS IN THE SOFTWARE.

Authors:
	aermicioi
**/
module aermicioi.aedi.test.container.inheritance_container;

import aermicioi.aedi.test.fixture;
import aermicioi.aedi.container.interface_container;
import aermicioi.aedi.container.singleton_container;
import aermicioi.aedi.exception.not_found_exception;
import std.exception;

unittest {
    InheritanceContainer container = new InheritanceContainer(new SingletonContainer);
    
    assertThrown!NotFoundException(container.get("unknown"));
    
    container.set(new MockFactory!MockObject, "mock");
    container.link("mock", "alias");
    
    assert(container.resolve("alias") == "mock");
    container.unlink("alias");
    assert(container.resolve("alias") != "mock");
    
}

unittest {
    import std.range;
    import std.conv;
    import std.traits;
    import aermicioi.aedi.test.fixture;
    
    SingletonContainer singleton = new SingletonContainer;
    InheritanceContainer container = new InheritanceContainer(singleton);
    
    container.set(new MockFactory!Person(), "mockObject");
    container.set(new MockFactory!Company(), "mockObject1");
    container.set(new MockFactory!Job(), "mockObject2");
    container.set(new CircularFactoryMock!Job().locator(container), "mock");
    
    container.link("mockObject", fullyQualifiedName!Person);
    container.link("mockObject1", fullyQualifiedName!Company);
    container.link("mockObject2", fullyQualifiedName!Job);

    assertThrown(container.instantiate());
    container.remove("mock");
    assert(!container.has("mock"));
    
    container.instantiate();

    assert(container.get(fullyQualifiedName!Nameable) !is null);
    assert(container.get(fullyQualifiedName!Payable) !is null);
    assert(container.get(fullyQualifiedName!Nameable) == container.get("mockObject"));
    assert(container.get(fullyQualifiedName!Nameable) != container.get("mockObject1"));
    assert(container.get(fullyQualifiedName!Payable) == container.get("mockObject2"));
    container.remove("mockObject");
    assert(container.get(fullyQualifiedName!Nameable) !is null);
    assert(container.get(fullyQualifiedName!Nameable) == container.get("mockObject2"));
    
    assertNotThrown(container.get(fullyQualifiedName!Nameable));
}